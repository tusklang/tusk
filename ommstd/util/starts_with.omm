include `ommstd/util/substr.omm`

var starts_with = fn(string -> str, string -> starts) {

    if (len(starts) > len(str)) return false

    startlen := len(starts)
    return rmend(str, startlen) == starts
}