pub stat fn curryrecieve(a: fn(i32) i32) i32 {
    return a(10); //call the parameter `a` which is a first class function and return
};

pub stat fn samplefn(a: i32) i32 {
    return a + 120; //sample code in here
};

pub stat fn curriedfn() i32 {
    return curryrecieve(samplefn);
};