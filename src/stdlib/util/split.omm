var split


ovld split = fn(string -> str, string -> del) {
    each (str, k, v) {
        
    }
}

ovld split = fn(string -> str, rune -> del) {
    
    splitted := []

    log str

    each (str, k, v) {
        if (v = del) splitted = append(splitted, "")

        splitted::(len(splitted) - 1) = splitted::(len(splitted) - 1) + v
    }

    return splitted
}