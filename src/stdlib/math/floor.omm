ns::Math {

  import ~ "?~/math/ceil.omm";

  proc ~ floor(num) {

    if (num < 0) return -1 * ceil(-1 * num);

    floored: 0;

    loop (num >= 1) {
      floored++;
      num--;
    }

    return floored;
  }

}
