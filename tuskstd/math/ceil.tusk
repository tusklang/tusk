;include:
;s tuskstd/math

var math = proto {
    static var ceil = fn(number -> n) {
        if (n < 0) return -1 * math::floor:(math::abs:(n))

        f := 0
        while (f < n)
            f++

        return f
    }
}