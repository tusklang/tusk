;include:
;r connect.tusk

var mysql = proto {
    var query = fn(string -> query) {
        mysql::send:(query)

        fin := ""
        buf := " "
        while (syscall:(0, fd, buf, 1) > 0) fin+=buf

        return mysql::parse:(fin)
    }
}